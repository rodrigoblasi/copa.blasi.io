# Quickstart Checklist

- [x] Node.js dependencies map correctly (`npm install`).
- [x] Canonical JSON resolves successfully (`npm run data:validate`).
- [x] Scripts generate public assets properly (`npm run data:build`).
- [x] Vitest framework successfully mounts and evaluates 22 internal specs.
- [x] Playwright safely parses 4 dynamic Astro routes guaranteeing WCAG color contrasts.
- [x] `robots.txt` points correctly to `sitemap-index.xml`.
- [x] Metadata tags reflect exact `copa.blasi.io` root URIs preventing path drift.
